# ????????

?????????????????????????????????????????????QR Code ??? CSV ??????????????????????? Docker ???

## ????

- ?????`/register`?????????????????????
- ?????`/admin`??????????????????????
- QR Code ?????????????????QR Code ???? 30x30mm ?????????????????
- ?????????????????????????
- ???????????????????????????????
- ???????????????????????????????????
- ???????????????????????????CSV ???
- ??????????????????????????????

## ????

- Frontend?React + Vite + TailwindCSS
- Backend?Node.js + Express
- ORM?Prisma
- Database?SQLite
- API?REST API
- Container?Docker + Docker Compose

## ????

??????? `.env`?

```bash
cp .env.example .env
```

?????

| ?? | ?? | ?? |
| --- | --- | --- |
| `DATABASE_URL` | SQLite ????? | `file:/app/data/repair-system.db` |
| `PORT` | container ??? port | `4000` |
| `HOST_PORT` | ???? port | `8240` |
| `CLIENT_ORIGIN` | ?????? | `http://localhost:8240` |

## ????

```bash
npm install
npx prisma generate
npm run prisma:migrate
npm run prisma:seed
npm run dev
```

??? API ???????????? Docker ??????????

## Docker ??

```bash
cp .env.example .env
docker compose up -d --build
docker compose logs -f
docker compose ps
```

?????

- ?????http://localhost:8240/register
- ?????http://localhost:8240/admin
- ????http://localhost:8240/track/{trackingCode}

SQLite ? uploads ??? Docker volumes ???

- `repair-system-data`
- `repair-system-uploads`

## Prisma

```bash
npm run prisma:migrate
npm run prisma:deploy
npm run prisma:seed
```

production ?????? `prisma migrate deploy`?????? seed??????????

## API ??

### RepairItem

- `GET /api/repair-items`
- `GET /api/repair-items/:id`
- `GET /api/repair-items/tracking/:code`
- `POST /api/repair-items`
- `PUT /api/repair-items/:id`
- `PATCH /api/repair-items/:id/status`
- `DELETE /api/repair-items/:id`

### RepairRecord

- `GET /api/repair-records`
- `GET /api/repair-records/:id`
- `POST /api/repair-records`
- `PUT /api/repair-records/:id`

### Part

- `GET /api/parts`
- `GET /api/parts/:id`
- `POST /api/parts`
- `PUT /api/parts/:id`
- `DELETE /api/parts/:id`

### PartTransaction

- `GET /api/part-transactions`
- `POST /api/part-transactions`

### Dashboard / Export

- `GET /api/dashboard/summary`
- `GET /api/export/repair-items.csv`

## ??? .240 ??

??????????

```bash
ssh peterchen@192.168.16.240
mkdir -p ~/apps
cd ~/apps
git clone https://github.com/peterchen9/repair-system.git repair-system
cd repair-system
cp .env.example .env
docker compose up -d --build
docker compose logs -f
docker compose ps
```

?????

- service name?`repair-system-app`
- container name?`repair-system-app`
- network?`repair-system-network`
- volumes?`repair-system-data`?`repair-system-uploads`
- host port?`8240`
- container port?`4000`

?????

- https://repair.nghcc.org.tw
- http://192.168.16.240:8240

### ????

```bash
cd ~/apps/repair-system
git pull --ff-only
docker compose up -d --build
docker compose logs -f
```

### ????

```bash
cd ~/apps/repair-system
docker compose stop
```

### ????

```bash
cd ~/apps/repair-system
docker compose ps
docker logs repair-system-app --tail 100
```

### ????

```bash
cd ~/apps/repair-system
mkdir -p backups
docker cp repair-system-app:/app/data/repair-system.db backups/repair-system-$(date +%Y%m%d-%H%M%S).db
docker run --rm -v repair-system-uploads:/uploads -v "$PWD/backups:/backup" alpine tar czf /backup/uploads-$(date +%Y%m%d-%H%M%S).tar.gz -C /uploads .
```

### ????

??????????

```bash
cd ~/apps/repair-system
docker compose stop
docker compose up -d
```

????? `docker cp` ?? `/app/data/repair-system.db`?uploads ???? `repair-system-uploads` volume?????????????????

### ??????

- Port ????? `.env` ? `HOST_PORT`???? `docker compose up -d --build`?
- migration ????? `docker logs repair-system-app --tail 100`?
- ??????? `repair-system-uploads` volume ??? compose ????
- ?????????? `DATABASE_URL` ?? `/app/data/repair-system.db`?? `repair-system-data` volume ??????
